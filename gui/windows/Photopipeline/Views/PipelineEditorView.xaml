<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Photopipeline.Views.PipelineEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Photopipeline.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right"
                    Background="Transparent" OverflowButtonVisibility="Collapsed">
            <AppBarButton Icon="Add" Label="Add Node" ToolTipService.ToolTip="Add a plugin node to the pipeline" />
            <AppBarButton Icon="Delete" Label="Delete" ToolTipService.ToolTip="Remove selected node" />
            <AppBarButton Icon="Copy" Label="Duplicate" ToolTipService.ToolTip="Duplicate selected node" />
            <AppBarSeparator />
            <AppBarButton Icon="ZoomIn" Label="Zoom In" />
            <AppBarButton Icon="ZoomOut" Label="Zoom Out" />
            <AppBarButton Icon="Zoom" Label="Fit All" ToolTipService.ToolTip="Fit all nodes in view" />
            <AppBarSeparator />
            <AppBarButton Icon="Sync" Label="Validate" ToolTipService.ToolTip="Validate pipeline connections" />
        </CommandBar>

        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollMode="Auto"
                      VerticalScrollMode="Auto">
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    CornerRadius="8"
                    MinWidth="800"
                    MinHeight="400">
                <Grid x:Name="CanvasRoot">
                    <Grid.Background>
                        <SolidColorBrush Color="#1A1A2E" />
                    </Grid.Background>
                    <controls:DAGCanvas x:Name="DagCanvas" />
                </Grid>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
