<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Photopipeline.Views.FilmstripView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="clr-namespace:Photopipeline.Models">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8,8,8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Images"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1" ToolTip="Add Images"
                    Width="32" Height="32" Padding="0" Margin="4,0"
                    Command="{Binding AddImageCommand}">
                <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets"
                           FontSize="14" HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
            </Button>
            <Button Grid.Column="2" ToolTip="Remove Selected"
                    Width="32" Height="32" Padding="0"
                    Command="{Binding RemoveImageCommand}">
                <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets"
                           FontSize="14" HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
            </Button>
        </Grid>

        <Button Grid.Row="1" Margin="8,0,8,8"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                Command="{Binding RunPipelineCommand}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets"
                           FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBlock Text="Process All" />
            </StackPanel>
        </Button>

        <ListView Grid.Row="2"
                  x:Name="FilmstripList"
                  SelectionMode="Extended"
                  ItemsSource="{Binding Images}"
                  SelectedItem="{Binding SelectedImage}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:ImageEntry}">
                    <Grid Height="72" Margin="4,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="48" Height="48"
                                CornerRadius="4"
                                Background="#E0E0E0">
                            <Image x:Name="ThumbnailImage"
                                   Source="{x:Null}"
                                   Stretch="UniformToFill"
                                   Width="48" Height="48" />
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center"
                                    Margin="8,0,0,0">
                            <TextBlock Text="{Binding FileName}"
                                       Style="{StaticResource BodyTextBlockStyle}"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="—"
                                       Style="{StaticResource CaptionTextBlockStyle}" />
                        </StackPanel>

                        <Border Grid.Column="2" Width="8" Height="8" CornerRadius="4"
                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                Margin="0,6">
                            <Border.Resources>
                                <SolidColorBrush x:Key="NoneBrush" Color="#404040" />
                                <SolidColorBrush x:Key="OriginalBrush" Color="#60CD18" />
                                <SolidColorBrush x:Key="OverriddenBrush" Color="#FF8C00" />
                                <SolidColorBrush x:Key="ErrorBrush" Color="#E81123" />
                            </Border.Resources>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
