<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Photopipeline.Views.FilmstripView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8,8,8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Images" Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1" ToolTipService.ToolTip="Add Images"
                    Width="32" Height="32" Padding="0" Margin="4,0"
                    HorizontalAlignment="Right">
                <SymbolIcon Symbol="Add" />
            </Button>
            <Button Grid.Column="2" ToolTipService.ToolTip="Remove Selected"
                    Width="32" Height="32" Padding="0"
                    HorizontalAlignment="Right">
                <SymbolIcon Symbol="Delete" />
            </Button>
        </Grid>

        <Button Grid.Row="1" Margin="8,0,8,8"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <SymbolIcon Symbol="Play" />
                <TextBlock Text="Process All" />
            </StackPanel>
        </Button>

        <ListView Grid.Row="2"
                  x:Name="FilmstripList"
                  SelectionMode="Extended"
                  IsItemClickEnabled="True"
                  ItemContainerStyle="{StaticResource DefaultListViewItemStyle}"
                  ScrollViewer.HorizontalScrollMode="Disabled"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Grid Height="72" Margin="4,2" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="48" Height="48"
                                CornerRadius="4"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                            <Image x:Name="ThumbnailImage"
                                   Source="{Binding}"
                                   Stretch="UniformToFill"
                                   Width="48" Height="48" />
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="image_0001.tif"
                                       Style="{StaticResource BodyTextBlockStyle}"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="6000x4000 · 72MB"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>

                        <Border Grid.Column="2" Width="8" Height="8" CornerRadius="4"
                                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,6">
                            <Border.Resources>
                                <SolidColorBrush x:Key="NoneBrush" Color="#404040" />
                                <SolidColorBrush x:Key="OriginalBrush" Color="#60CD18" />
                                <SolidColorBrush x:Key="OverriddenBrush" Color="#FF8C00" />
                                <SolidColorBrush x:Key="ErrorBrush" Color="#E81123" />
                            </Border.Resources>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
