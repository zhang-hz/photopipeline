<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Photopipeline.Views.PluginPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Plugin Settings"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   Margin="8,8,8,4" />

        <AutoSuggestBox Grid.Row="1" x:Name="PluginSearchBox"
                        PlaceholderText="Search plugins..."
                        Margin="8,0,8,8"
                        QueryIcon="Find" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="PluginListPanel" Spacing="4" Margin="8,0">
                <TextBlock Text="No plugin selected"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="Visible"
                           x:Name="NoPluginText"
                           Margin="0,20,0,0"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Margin="8,8,8,12" Spacing="8">
            <Button x:Name="ApplyButton"
                    Content="Apply Parameters"
                    Style="{StaticResource AccentButtonStyle}"
                    HorizontalAlignment="Stretch" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button x:Name="ResetButton"
                        Content="Reset"
                        Style="{StaticResource DefaultButtonStyle}" />
                <Button x:Name="PreviewButton"
                        Content="Preview"
                        Style="{StaticResource DefaultButtonStyle}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
