[package]
name = "photopipeline-halide"
version.workspace = true
edition.workspace = true

[features]
default = []
halide-runtime = []

[dependencies]
photopipeline-core = { path = "../core" }
thiserror.workspace = true

[build-dependencies]
pkg-config = "0.3"
cmake = "0.1"
